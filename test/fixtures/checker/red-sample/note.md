# 违规样本（红样本）

这份文档**故意**含一条本机盘卷路径，用来证明检查器会开火：

参考配置在 D:\opt\demo\config.json（本机路径，公开仓不该出现）。

> 为什么这个样本能放进仓库：`test/fixtures/**` 在 `rk-selfcheck` 的 S8 扫描里是**显式豁免**的
> （夹具里的历史文本属于被测输入，不是对外文档）。判据见 `src/selfcheck.mjs` 的 S8 段。
