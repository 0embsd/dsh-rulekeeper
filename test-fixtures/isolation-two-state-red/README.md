# 两态样本 · 红态：旧口径（改动前）

- 被测纪律：`CONCURRENCY-IO` —— 用例不得写真实落点。
- 旧口径（红）：隔离只要求"用例自己记得传隔离 env"（靠自觉），没有机制面 ⇒ 只要有一处调用忘了传
  env，测试会话键（`sc-*`）就会写进**真实**用户级落点，把真实会话的去重记录挤掉。
  真实事故（两次）：8 个测试会话键被写进真实用户级 `usage.json`。
- 新口径（绿）：见 `isolation-two-state-green`。
- 可重跑的构造方式：检查器 `scripts/checkers/test-isolation.mjs` 每次运行都会**现造**一棵
  "直取真实用户级落点 + 同文件有写动作"的临时样本树，并断言它必须被判红。
