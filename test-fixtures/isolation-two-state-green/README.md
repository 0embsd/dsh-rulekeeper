# 两态样本 · 绿态：新口径（改动后）

- 被测纪律：`CONCURRENCY-IO` —— 用例不得写真实落点。
- 新口径（绿）：判据落在"**真实落点会不会被这次用例写到**"这个对象事实上，三条一起判：
  ①构造落点时直取真实用户级落点（`DSH_HOME` / `homedir()`+`.dsh`）**且有写动作** ⇒ 红；
  ②有写动作却没调 `isolateProcessUserLanding()` ⇒ 红；③同上的 `homedir` 形态 + 写动作 ⇒ 红。
- 反面校准（写检查器时实测）：第一版把"导入了 helper 却没调隔离"**一律**判红，在真仓上一次报出
  58 条，绝大多数只是**只读**用例（根本不会写到真实落点）——那是拿函数名当判据。收紧后真仓 0 条。
- 旧口径红样本：见 `isolation-two-state-red`。
