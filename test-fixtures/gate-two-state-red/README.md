# 两态样本 · 红态：旧口径（改动前）

这份样本是**旧口径红**的可重跑载体（规则 42：判据的红态样本必须能在任何时刻重跑）。

- 被测纪律：`GATE-DISCIPLINE` —— 收尾门禁四件齐备且真能跑。
- 旧口径（红）：hook 只看"文件在不在"，不看 ①行尾是否纯 LF ②索引 mode 是否 100755 ③是否把自己的
  名字作为 `hook.mjs` 的子命令传下去 ⇒ 装上了但**等于没装**（POSIX 上 CRLF 或非可执行会让 git
  直接忽略该 hook）。
- 新口径（绿）：见 `gate-two-state-green`。
- 可重跑的构造方式：检查器 `scripts/checkers/gate-finality.mjs` 每次运行都会**现造**一棵
  "CRLF + 缺 `.githooks/*` 钉规则"的临时样本树，并断言它必须被判红；造不出来或没判红都算违规。
  因此这条判据不依赖"此刻恰好处于违规态"。
