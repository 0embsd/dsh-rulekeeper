# 两态样本 · 绿态：新口径（改动后）

- 被测纪律：`CAT-TECH` —— deny 列表不得为测试方便开例外。
- 新口径（绿）：`src/**` 的豁免/放行表里**不得**出现 `test/` 路径（deny 列表不为测试开口子）；
  正解是让夹具的"可疑串"**运行时拼装**（`const TOOL_NAME = 'myx' + '-secret-branch'`），
  这样运行时是真正的可疑串、源码正文里不构成那个 token。
- 边界（诚实声明）：本条只判"新的测试例外有没有被写进豁免表"，不判"某个串到底该不该判红"
  （后者由 S8 自己 + 运行时构造的夹具负责）。
- 旧口径红样本：见 `exception-two-state-red`。
