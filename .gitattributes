# dsh-rulekeeper · 行尾与文本属性纪律（LF-250/260 实测换来的）
#
# **本文件自己**也必须钉住（P18 实测：`gitattributes` 没有扩展名，按扩展名的规则管不到它；
# 当时它自己就带着 1 处 CRLF，而旧的扩展名白名单让检查器**从来没看过它**）。
# 模式写文件自己的名字即可：# 这一行会先被读到，再作用于本文件。
# **本文件自己**也必须钉住。P18 实测：`.gitattributes` 没有扩展名，按扩展名的规则管不到它；当时它自己就带着 1 处 CRLF，而旧的扩展名白名单让检查器**从来没看过它**。
# ⚠ 模式必须写**带前导点的全名**（或 `*`）：本仓的属性表按 basename 匹配，`gitattributes` / `*.gitattributes`
# / `/gitattributes` 三种写法实测**静默不生效**（与 .gitignore 的直觉相反，6 组受控实验验证）。
.gitattributes  text eol=lf
#
# 为什么必须有这个文件：`test/fixtures/expected/*.json` 是**逐字基准**，
# 6 份基准与 7 个夹具都依赖"必须纯 LF"。在 autocrlf=true 的 Windows 机器上 checkout
# 会把它们变成 CRLF → 逐字比对全红，而 TextDiff 肉眼看不出来（LF-250 真实踩过：
# 12 处 CRLF 致 6 例全红）。
*.json   text eol=lf
*.md     text eol=lf
*.mjs    text eol=lf
*.txt    text eol=lf
*.ps1    text eol=lf
# 夹具一律按二进制对待：不让 git 做任何行尾转换（字节即判据）
test/fixtures/**  -text

# git hook 脚本**没有扩展名**（pre-commit / post-commit / pre-push）⇒ 上面按扩展名钉 LF 的规则管不到它们。
# 现场（2026-09-21）：autocrlf=true 的机器上 checkout 会把它们转成 CRLF ⇒ ①与 hooks.json 的 sha256 清单不符
# （verify 判 HOOK_MODIFIED）②POSIX 上 `#!/bin/sh\r` 直接坏掉 ⇒ 闸门变哑。显式钉 LF，别再靠机器配置。
.githooks/*  text eol=lf

# 红态/两态**样本树**一律按二进制对待：不让 git 做任何行尾转换（字节即判据）。
# 为什么必须显式钉：`test-fixtures/red/.githooks/pre-commit` 是**故意写 CRLF** 的违规样本，
# 而 `test-fixtures/red/test/**` 是**故意违规**的用例样本 —— 一转换，样本就不再是"那个违规现场"。
test-fixtures/**  -text