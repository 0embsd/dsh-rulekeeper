{
  "schema": 1,
  "rule": "CAT-VERIFY",
  "checkerRef": "@self/scripts/checkers/misreport-surface.mjs",
  "command": [
    "node",
    "scripts/checkers/misreport-surface.mjs"
  ],
  "expectRed": {
    "exitCode": 1
  },
  "expectGreen": {
    "exitCode": 0
  },
  "redSample": {
    "kind": "tree",
    "source": "test-fixtures/misreport-red",
    "note": "被检对象=该目录（内含 .dsh-ai/rulekeeper/rules.json 与 fake.spec.json）；假检查器恒定非 0 ⇒ A 判据必红"
  },
  "greenSample": {
    "kind": "tree",
    "source": "test-fixtures/misreport-green",
    "note": "入库夹具（2026-09-23 改，原为 \".\"）。原口径是**活体工作区** ⇒ 有在途改动时绿样本身份漂移，实测报过一次 MISREPORT_GREEN_FALSE_POSITIVE（并发写落点期间），复跑即绿 —— 规则 42 同族：样本必须随时可重跑。⚠ 夹具**必须让本检查器真能核出结论**：第一版夹具是个空绑定落点，于是它在本检查器的绿样本上 exit=2（'没有可核对象'）⇒ 误报面判红（rk-effect verify 当场抓到）。现在的夹具自带一条 checker 绑定 + 假检查器（按样本目录里有无 FAIL 决定退出码）+ 红/绿两棵样本树 ⇒ 它在本检查器的绿样本上得出\"0 违规\"。命令相对**夹具根**（本检查器用 cwd=被检根 跑命令，这点是实测出来的）。"
  },
  "checkerVersion": "misreport-surface@1",
  "timeoutMs": 30000,
  "notes": "误报面：对 rules.json 里每条 checker 绑定核「绿样本上必须 exit=0 / 红样本上必须非 0 / 必须写明绿样本」。规则 53 的机械面。递归护栏：命令里含本检查器的绑定跳过并计数（不判自己）。",
  "applicability": {
    "scope": "plugin-repo-only",
    "requires": [
      "scripts/checkers/misreport-surface.mjs 在 cwd 下",
      "rules.json 里有 checker 绑定"
    ],
    "note": "**不可搬迁**：它按 cwd 解析各绑定的 spec 与 command，要求\"项目根相对路径\"这套前提成立 ⇒ 只适合插件仓自身（及把检查器整套搬过去的仓）。无 rules.json ⇒ exit 2"
  }
}
