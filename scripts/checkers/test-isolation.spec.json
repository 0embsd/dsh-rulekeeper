{
  "schema": 1,
  "rule": "CONCURRENCY-IO",
  "checkerRef": "@self/scripts/checkers/test-isolation.mjs",
  "command": [
    "node",
    "scripts/checkers/test-isolation.mjs"
  ],
  "expectRed": {
    "exitCode": 1
  },
  "expectGreen": {
    "exitCode": 0
  },
  "redSample": {
    "kind": "tree",
    "source": "test-fixtures/red",
    "note": "被检对象=当前项目根；红态样本由检查器每次现造（见脚本头部注释）"
  },
  "greenSample": {
    "kind": "tree",
    "source": "."
  },
  "checkerVersion": "test-isolation@1",
  "timeoutMs": 20000,
  "notes": "用例不得写真实落点：判据钉在\"真实落点来源 + 写动作\"同处，且整套用例必须有进程级隔离机制面（isolateProcessUserLanding）。红态样本每次现造。",
  "applicability": {
    "scope": "js-project-with-tests",
    "requires": [
      "test/**/*.mjs"
    ],
    "note": "**仅适用于有 JS 用例树的工程**：判\"用例不得写真实落点\"。无 .mjs 用例树 ⇒ exit 2（2026-09-21 修正：此前返回 0，会在无对象时给出**空转绿**）"
  }
}
